/**
 * CONFIGURATION
 *
 * IMPORTANT SECURITY NOTE:
 * These settings are exposed in the browser.
 * For a production environment, it is highly recommended to:
 * 1. Use Supabase Auth for the admin panel instead of a hardcoded password.
 * 2. Set up Row Level Security (RLS) in your Supabase dashboard to protect your data.
 */

const CONFIG = {
    SUPABASE_URL: 'YOUR_SUPABASE_URL',
    SUPABASE_ANON_KEY: 'YOUR_SUPABASE_ANON_KEY',
    ADMIN_PASSWORD: 'rudra123', // WARNING: Change this and use secure Auth for production
    WA_NUMBER: '9779823783486'
};
